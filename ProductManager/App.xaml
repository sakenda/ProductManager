<Application x:Class="ProductManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ProductManager"
    StartupUri="Views/NewMain.xaml">
    <Application.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Resource/Btn_Blue.xaml"/>
                <ResourceDictionary Source="/Views/Resource/Colors.xaml"/>
                <ResourceDictionary Source="/Views/Resource/ListView.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Segoe MDL2"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{StaticResource dSchriftv1}"/>
            </Style>

            <Style TargetType="Page">
                <Setter Property="FontFamily" Value="Segoe MDL2"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{StaticResource dSchriftv1}"/>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="10"/>
                <Setter Property="Padding" Value="10 0 0 0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="{StaticResource dSchriftv1}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="TextBox.Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="3" Direction="330" Color="Black" Opacity="0.5" BlurRadius="4"/>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>

<!-- Mask nutzen um bilder einzufärben
        <Rectangle Width="30" Height="30">
            <Rectangle.Fill>
                <SolidColorBrush Color="Red"/>
            </Rectangle.Fill>
            <Rectangle.OpacityMask>
                <ImageBrush ImageSource="/Shared/Icons/save_black.png"/>
            </Rectangle.OpacityMask>
        </Rectangle>
        -->